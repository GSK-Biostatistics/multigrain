# multigrain_verbosity() validates the value it finds

    Code
      multigrain_verbosity()
    Condition
      Error in `multigrain_verbosity()`:
      ! Option "multigrain_verbosity" must be one of: info, detail, or silent.

---

    Code
      multigrain_verbosity()
    Condition
      Error in `multigrain_verbosity()`:
      ! Option "multigrain_verbosity" must be one of: info, detail, or silent.

---

    Code
      multigrain_verbosity()
    Condition
      Error in `multigrain_verbosity()`:
      ! Option "multigrain_verbosity" must be one of: info, detail, or silent.

