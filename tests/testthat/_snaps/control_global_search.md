# control_global_search is deprecated

    Code
      control_global_search(ctrl, FALSE)
    Condition
      Error:
      ! `control_global_search()` was deprecated in multigrain 0.3.0 and is now defunct.
      i Please use the `global_search` `graph_optimise()` argument instead.

