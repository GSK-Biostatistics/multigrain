% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_constraint.R
\name{graph_constraint_free}
\alias{graph_constraint_free}
\title{Create an unconstrained \emph{graph constraint}}
\usage{
graph_constraint_free(num_hyp, names = "auto")
}
\arguments{
\item{num_hyp}{An integer denoting the number of hypotheses. Must be greater
than or equal to 2.}

\item{names}{An optional character vector containing hypotheses' names. If
not provided it defaults to \code{"auto"} meaning the hypotheses will be
automatically named \code{"H1"}, \code{"H2"}, etc.}
}
\value{
An unconstrained \code{multigrain_graph_constraint} object. In
\code{hyp_constraint} all values are \code{NA} and similarly in the
\code{trans_constraint}, except for the diagonal which is set to \code{0}. If we have
only 2 hypotheses, then the matrix will have 0 on the diagonal and the
other 2 elements are set to 1.
}
\description{
Create a graph constraint that allows for all hypotheses transitions to be
optimised.
}
\examples{
# Create a graph constraint object with 3 hypotheses and no constraints
graph_constraint_free(3)

# Create a graph constraint object with 2 hypotheses results in set values in
# the transition matrix
graph_constraint_free(2)
}
