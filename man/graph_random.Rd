% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_graph.R
\name{graph_random}
\alias{graph_random}
\title{Generate random graph}
\usage{
graph_random(m = NULL, graph_constraint = NULL, names = "auto")
}
\arguments{
\item{m}{An integer representing the number of hypotheses. It defines both
the length of the weight vector and the dimensions (\verb{m x m}) of the
transition matrix. Optional when \code{graph_constraint} is supplied (inferred
from constraint dimensions). If both \code{m} and \code{graph_constraint} are
supplied, they must agree.}

\item{graph_constraint}{An optional graph constraint object created by
\code{\link[=graph_constraint]{graph_constraint()}}. When supplied, fixed elements are honoured and only
free (\code{NA}) positions are randomised.}

\item{names}{An optional character vector containing hypotheses' names. If
not provided it defaults to \code{"auto"} meaning the hypotheses will be
automatically named \code{"H1"}, \code{"H2"}, etc.}
}
\value{
A list containing:
\itemize{
\item \code{hyp_weight}: A numeric vector of length \code{m} representing the generated
hypothesis weights.
\item \code{trans_matrix}: A numeric matrix of dimension \verb{m x m} representing the
generated transition matrix.
}
}
\description{
Generate a random graph, consisting of a vector of hypothesis weights and a
transition matrix. The weights and transition matrix are randomly generated
respecting any constraints provided, ensuring that the sum of the weights
equals 1 and each row of the transition matrix sums to 1.
}
\examples{
# Generate a random graph for 5 hypotheses
random_graph <- graph_random(5)
print(random_graph$hyp_weight)  # Prints the weight vector
print(random_graph$trans_matrix)  # Prints the transition matrix

# Generate a random graph respecting constraints
gc <- graph_constraint(
    hyp_constraint = c(0.5, NA, NA),
    trans_constraint = matrix(c(0, NA, NA, NA, 0, NA, NA, NA, 0), 3, 3)
)
random_graph <- graph_random(graph_constraint = gc)
}
