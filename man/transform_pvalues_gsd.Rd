% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform_pvalues_gsd.R
\name{transform_pvalues_gsd}
\alias{transform_pvalues_gsd}
\title{Transform group sequential p-values into repeated p-values}
\usage{
transform_pvalues_gsd(
  pvals,
  ...,
  info_frac = NULL,
  spending,
  alpha = 0.025,
  look_back = FALSE,
  grid_size = 1024L
)
}
\arguments{
\item{pvals}{A numeric array of raw p-values with dimensions
\code{c(nsim, m, K)}: simulations by hypotheses by analyses.}

\item{...}{These dots are for future extensions and must be empty.}

\item{info_frac}{Information fractions. Either a numeric vector of length
\code{K}, applied to every hypothesis, or an \code{m} by \code{K} numeric matrix with
\code{NA} at analyses where a hypothesis has no data. Defaults to the
\code{info_frac} attribute of \code{pvals}.}

\item{spending}{An alpha-spending function, or a list of \code{m} of them (one
per hypothesis). A spending function takes a level and a vector of
information fractions and returns the cumulative level spent, either as a
plain numeric vector or as an object with a \code{spend} element (as the
\code{gsDesign} spending functions do). It must spend the whole level by full
information.}

\item{alpha}{A number giving the overall one-sided significance level.
Default is \code{0.025}.}

\item{look_back}{A logical of length 1 or \code{m}. \code{TRUE} switches a hypothesis
to sequential p-values (see Details). Default is \code{FALSE}.}

\item{grid_size}{A whole number giving the number of allocated levels in
each boundary table. Default is \code{1024}.}
}
\value{
A \code{multigrain_pvals_gsd} object made up of:
\itemize{
\item \code{pvals}: the transformed array, of the same dimensions as the input.
\item \code{nsim}, \code{m}, \code{K}: simulations, hypotheses and analyses.
\item \code{alpha}: the level the boundary tables were built up to.
\item \code{info_frac}: the \code{m} by \code{K} matrix of information fractions.
\item \code{look_back}: the per-hypothesis semantics actually used.
\item \code{spending}: a label per spending function.
\item \code{tables}: per hypothesis, the analyses that carry distinct information,
the analysis at which it matured, and the grid of allocated levels with
the nominal boundaries computed at each of them.
}
}
\description{
\code{transform_pvalues_gsd()} converts an array of nominal (raw) p-values from a
group sequential design into \emph{repeated} p-values: for each hypothesis and
each analysis, the smallest level at which that p-value would cross its
alpha-spending boundary. Because the boundary is monotone in the allocated
level, the group sequential rejection rule "\eqn{p_{i,k}} below the boundary
for level \eqn{w_i \alpha}" becomes the fixed-sample rule
"\eqn{p^r_{i,k} \le w_i \alpha}", so a graphical procedure can be run on the
transformed values without ever evaluating a boundary.
}
\details{
Boundaries are computed with \code{\link[gsDesign:gsBound1]{gsDesign::gsBound1()}} on a log-spaced
grid of allocated levels running from \code{1e-14} up to \code{alpha}, and the
resulting monotone table is inverted by log-log linear interpolation.

Per hypothesis, writing \eqn{D} for the analyses with a non-missing
information fraction and \eqn{k^{mat}} for the first of those that reaches
full information (or the last analysis in \eqn{D} if none does):
\itemize{
\item an analysis without data (\code{NA} information fraction) before
\eqn{k^{mat}} is given a repeated p-value of 1, i.e. "cannot reject";
\item analyses after \eqn{k^{mat}} copy the repeated p-value of \eqn{k^{mat}}
forward, because the statistic of a matured endpoint does not change (it
may still be rejected later if recycling gives it more alpha). A
non-missing raw p-value supplied after maturity is ignored, with a
warning if it differs from the maturity value;
\item a hypothesis with a single analysis at full information is returned
unchanged, since there the boundary \emph{is} the level;
\item if \code{look_back} is \code{TRUE} for a hypothesis its repeated p-values are
replaced by their running minimum over analyses (the \emph{sequential}
p-value), which allows a rejection on the strength of the evidence at
any earlier analysis.
}

If a boundary table is not non-decreasing in the allocated level the
spending function is not "well ordered", the group sequential graphical
procedure is not valid for it, and the transform aborts.
}
\examples{
# two hypotheses analysed at 60\% and 100\% of the planned information
set.seed(1)
raw <- array(stats::runif(100 * 2 * 2), dim = c(100L, 2L, 2L))

pvals_gsd <- transform_pvalues_gsd(
    raw,
    info_frac = c(0.6, 1),
    spending = gsDesign::sfLDOF,
    grid_size = 128L
)

pvals_gsd
}
