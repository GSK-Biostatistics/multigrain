% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_power_gsd.R
\name{calc_power_pvals_gsd}
\alias{calc_power_pvals_gsd}
\title{Calculate power for a group sequential graph-based multiple test procedure}
\usage{
calc_power_pvals_gsd(
  pvals,
  hyp_weight,
  trans_matrix,
  ...,
  alpha = NULL,
  custom_power = NULL,
  sum_to_one_constraint = TRUE,
  call = rlang::caller_env()
)
}
\arguments{
\item{pvals}{A \code{multigrain_pvals_gsd} object of repeated (or, per
hypothesis, sequential) p-values, created with \code{\link[=transform_pvalues_gsd]{transform_pvalues_gsd()}}.}

\item{hyp_weight}{A numeric vector representing the weights of the nodes
(hypotheses) in the graph. All elements must be in the interval [0, 1]
and the vector must sum to 1.}

\item{trans_matrix}{A numeric matrix representing the transition matrix
between nodes. All elements must be in the interval [0, 1], with diagonal
elements equal to 0, and each row must sum to 1.}

\item{...}{These dots are for future extensions and must be empty.}

\item{alpha}{A single numeric value representing the overall one-sided
significance level to test at, or \code{NULL} (the default) to use the level
of \code{pvals}. A supplied value must be greater than \code{0} and at most the
level the repeated p-values were built up to.}

\item{custom_power}{A list of user-defined power measures. Alternatively a
single measure may be given on its own. Three kinds are accepted, and
each receives a different matrix:
\itemize{
\item a \code{multigrain_trial_success_gsd} object from \code{\link[=trial_success_gsd]{trial_success_gsd()}} is
evaluated by its compiled function on the integer matrix of decision
times (simulations by hypotheses, \code{0} for never rejected);
\item a fixed-sample \code{multigrain_trial_success} object from \code{\link[=trial_success]{trial_success()}}
is evaluated on the logical matrix of rejections, so a rejection-only
gain reports the same number here as \code{\link[=calc_power_pvals]{calc_power_pvals()}} would;
\item a plain function is called once per simulated trial with that trial's
integer vector of decision times, and the results are averaged.
}

If the list has no names, the measures are reported as \code{"func1"},
\code{"func2"}, and so on.}

\item{sum_to_one_constraint}{A logical value controlling whether to allow
graphs where transition matrix rows are not constrained to sum to one,
for example in a fixed sequence. Defaults to \code{TRUE}.}

\item{call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
A list containing:
\itemize{
\item \code{local_power}: the proportion of simulations in which each hypothesis
is rejected at any analysis.
\item \code{local_power_by_analysis}: an \code{m} by \code{K} matrix whose entry
\eqn{(i, k)} is the proportion of simulations in which hypothesis
\eqn{i} was rejected at analysis \eqn{k} or earlier. Its last column is
\code{local_power}.
\item \code{exp_rejections}: the expected number of rejections.
\item \code{disj_power}: the probability of rejecting at least one hypothesis.
\item \code{conj_power}: the probability of rejecting all hypotheses.
\item \code{mean_decision_look}: for each hypothesis, the mean analysis index over
the simulations in which it was rejected, and \code{NA} when it never was.
\item \code{time_distribution}: an \code{m} by \code{K + 1} matrix of proportions, with a
\code{"never"} column followed by one column per analysis. Its rows sum to
one, and one minus its \code{"never"} column equals \code{local_power} up to
floating-point rounding.
\item \code{"..."}: one entry per measure in \code{custom_power}.
}
}
\description{
Group sequential counterpart of \code{\link[=calc_power_pvals]{calc_power_pvals()}}. Applies a graph to an
object of repeated p-values built by \code{\link[=transform_pvalues_gsd]{transform_pvalues_gsd()}} and reports
power by hypothesis and by analysis, together with the distribution of the
analysis at which each hypothesis was declared rejected.
}
\details{
\code{calc_power_pvals_gsd()} runs the group sequential kernel once and
summarises both of its outputs: the rejection indicators and the decision
times (the analysis at which the procedure declared each hypothesis
rejected, \code{0} for never). Every quantity is on the analysis-index scale;
calendar times are not stored by the package.
}
\examples{
# two hypotheses, an interim at 60\% information and a final analysis
corr <- matrix(c(1, 0.5, 0.5, 1), nrow = 2)

set.seed(1)
raw <- simulate_pvalues_gsd(
    power_nominal = c(0.9, 0.8),
    corr_matrix = corr,
    info_frac = c(0.6, 1),
    nsim = 500
)

pvals <- transform_pvalues_gsd(
    raw,
    spending = gsDesign::sfLDOF,
    grid_size = 128L
)

# a rejection at the final analysis is worth 75\% of one at the interim
gain <- trial_success_gsd(
    d(t1) + d(t2),
    d = c(1, 0.75),
    verbose = "silent"
)

result <- calc_power_pvals_gsd(
    pvals,
    hyp_weight = c(0.5, 0.5),
    trans_matrix = matrix(c(0, 1, 1, 0), nrow = 2),
    custom_power = list(gain = gain)
)

result$local_power_by_analysis
result$time_distribution
result$gain
}
\seealso{
\code{\link[=calc_power_pvals]{calc_power_pvals()}} for the fixed-sample version and
\code{\link[=graph_optimise_gsd]{graph_optimise_gsd()}} for the optimiser.
}
