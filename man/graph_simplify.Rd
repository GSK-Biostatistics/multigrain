% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_simplify.R
\name{graph_simplify}
\alias{graph_simplify}
\title{Simplify an optimised graph within a trial success budget}
\usage{
graph_simplify(
  graph_optimal,
  pvals,
  ...,
  gain_tolerance = 0.001,
  alpha = NULL,
  global_search = TRUE,
  num_threads = 1L,
  control = NULL,
  verbose = multigrain_verbosity()
)
}
\arguments{
\item{graph_optimal}{A \code{multigrain_graph_optimal} object, as returned by
\code{\link[=graph_optimise]{graph_optimise()}}. This is the reference graph.

The trial success measure is compiled, and a compiled function does not
survive being saved and reloaded. \code{graph_simplify()} rebuilds a dead
function automatically from the objective stored on the object. A live
function is checked against that objective before it is used; inconsistent
or invalid objects produce an error.}

\item{pvals}{A numeric matrix of p-values, one row per simulated trial and
one column per hypothesis. It is supplied again because the optimised
object does not store it. It need not be the matrix used in stage 1: the
cap is always measured on the matrix given here, so \code{gain_reference} is
recomputed on it and may differ from the object's stored
\verb{$power$trial_success}.}

\item{...}{These dots are for future extensions and must be empty.}

\item{gain_tolerance}{A single number in \verb{[0, 1]}. The fraction of the
reference graph's trial success measure, on the supplied \code{pvals}, that may
be given up in total. Defaults to \code{1e-3}.}

\item{alpha}{A single numeric value representing the overall one-sided
significance level. Defaults to the value stored on \code{graph_optimal}; an
explicit value overrides it. Objects saved by a version that did not store
\code{alpha} must supply it here.}

\item{global_search}{A logical indicating whether to run the genetic
algorithm before the local optimisation. Defaults to \code{TRUE}.}

\item{num_threads}{Number of threads to use for parallel execution of the
shortcut algorithm. On shared systems (HPC clusters, login nodes), always
explicitly set \code{num_threads} based on your resource allocation. Default is
\code{1L} (serial execution).}

\item{control}{An optional \code{multigrain_control} object. Defaults to the
settings stored on \code{graph_optimal} with the genetic algorithm's \code{run}
halved, since the search starts in the right basin and does not need
stage 1's floor on cost. When the global search can run, the \code{GA::ga()}
options \code{mutation} and \code{suggestions} are reserved: simplification requires
its support-changing mutation and reference-first seed population. Setting
either option in an explicit \code{control} object is an error. If they are
inherited from \code{graph_optimal}, they are ignored with a warning. They are
irrelevant and left alone when no global search runs.}

\item{verbose}{An optional string controlling verbosity (\code{"detail"} >
\code{"info"} > \code{"silent"}). Verbosity can also be set at package level with the
\code{multigrain_verbosity} option (see \code{\link[=multigrain_verbosity]{multigrain_verbosity()}}):
\itemize{
\item \code{"info"} (default): will only show milestones / informational messages
highlighting the progress of the optimisation at coarse-grained level.
\item \code{"detail"}: will show milestones and information about fine-grained
optimisation events.
\item \code{"silent"}: no information about the progress of the optimisation is
printed to the console. Errors and warnings are still thrown normally.
}}
}
\value{
A \code{multigrain_graph_optimal} object, so that \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}},
\code{\link[=plot]{plot()}} and \code{\link[=calc_power_pvals]{calc_power_pvals()}} work on it unchanged. Its \code{sparsity}
element is a list containing:
\itemize{
\item \code{gain_tolerance}: the cap, as supplied.
\item \code{reference}: a list with the reference graph's \code{hyp_weight} and
\code{trans_matrix}, so the two graphs can be plotted side by side.
\item \code{gain_reference}: the reference graph's trial success on \code{pvals}.
\item \code{gain}: the returned graph's trial success, equal to
\verb{$power$trial_success}.
\item \code{gain_loss}: \code{gain_reference - gain}.
\item \code{gain_loss_fraction}: \code{gain_loss / gain_reference}.
\item \code{budget}: \code{gain_tolerance * gain_reference}.
\item \code{n_edges_reference}, \code{n_edges}: total non-zero transition entries,
including any pinned by the constraint, before and after.
\item \code{n_edges_free_reference}, \code{n_edges_free}: non-zero \emph{free} entries,
before and after.
\item \code{prune_loss}: the exact loss of trial success across the removals that
pruning accepted. Negative if pruning raised the measure.
\item \code{source}: \code{"global"}, \code{"local"} or \code{"reference"}, saying which stage
produced the returned graph.
}
}
\description{
Takes a graph from \code{\link[=graph_optimise]{graph_optimise()}} and searches for one with fewer edges,
giving up at most a stated fraction of its trial success measure. Optimised
graphs often carry several small transition weights that contribute almost
nothing but that a reviewer still has to reason about; \code{graph_simplify()}
trades a little expected gain for a graph that is easier to present.
}
\details{
The graph you pass in is the \emph{reference}. Its trial success measure is
recomputed on the \code{pvals} you supply, and the returned graph is guaranteed
to reach at least \code{(1 - gain_tolerance)} times that value on the same
simulated trials. Among the graphs that clear that floor, the search prefers
the one with fewest edges, and among graphs with the same number of edges,
the one with the highest trial success.

Family-wise error control is unaffected. A Bonferroni-based graphical
procedure controls the family-wise error rate strongly for any weights
summing to at most one and any transition matrix with non-negative entries,
zero diagonal and row sums at most one. Removing an edge and renormalising
its row preserves all three, so only the objective changes, never the class
of procedures searched.
\subsection{Edge removal during the global search}{

The mutation the genetic algorithm normally uses selects each parameter with
probability 0.1 and replaces it with a draw from a Cauchy distribution
centred on its current value. That moves weight around well, but it is a
poor way to \emph{remove} an edge: "removed" means the decoded entry falls below
\code{1e-5}, and a Cauchy draw lands there about once in 100,000 tries. Crossover
does not help, because a convex combination of two parents is zero only
where both parents are already zero, and the local search is a continuous
method that never aims for the threshold. Left alone, the search would
\emph{prefer} sparser graphs but almost never \emph{produce} one to compare.

So each mutation call now first tosses a coin. If it comes up, the call
performs a zeroing move instead, of one of two kinds. It either picks one
free transition parameter that is currently above the threshold and sets it
to exactly zero, removing that edge outright; or it picks a row and rescales
its parameters so that the row's \emph{derived} entry -- the one computed as one
minus the others, which no parameter can reach -- falls just below the
threshold. Without the second kind, one edge per row would be unreachable by
the global search and removable only by the pruning step at the end. If the
coin does not come up, or there is nothing left to zero, the call falls
through to the ordinary Cauchy perturbation.

With \code{global_search = FALSE} no genetic algorithm runs, the local search
starts from the reference, and edges are removed only by pruning. That is
cheaper, but it cannot find removals that only become affordable once the
remaining weights are re-tuned, nor move to a different topology.
}

\subsection{What to expect}{

Edge costs are lumpy, so the fraction of the budget actually spent varies:
the cap is the guarantee, the spend is whatever the graph allows. On a
four-hypothesis example with seven edges, a budget of \code{1e-3} bought one edge
for 9\% of the budget; at \code{1e-2} the same, because the next cheapest edge
cost more than the budget that remained; at \code{2e-2}, two edges for 61\% of it.
\code{gain_tolerance = 0} is well defined and useful: it asks for the fewest
edges among graphs whose trial success does not fall at all.

Called after \code{\link[=graph_optimise]{graph_optimise()}}, the whole two-stage run costs roughly 1.3
to 1.6 times a single optimisation. Trying several values of
\code{gain_tolerance} does not repeat the first optimisation.

Simplifying an already-simplified graph compounds the cap: the second call
measures its budget against the first call's result, not against the
original graph.
}
}
\examples{

pvals <- simulate_pvalues(
  power_nominal = c(0.9, 0.85, 0.8, 0.75),
  corr_matrix = diag(4),
  nsim = 5000
)

ts <- trial_success(r1 + r2 + r3 + r4)

\donttest{
optimal <- graph_optimise(
  pvals = pvals,
  graph_constraint = graph_constraint_free(4),
  trial_success = ts
)

# Give up at most 0.5\% of the trial success measure for a smaller graph
simpler <- graph_simplify(optimal, pvals, gain_tolerance = 5e-3)

simpler$sparsity$n_edges_reference
simpler$sparsity$n_edges
}
}
\seealso{
\code{\link[=graph_optimise]{graph_optimise()}} for the first-stage optimisation.
}
