% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimisation_gsd.R
\name{graph_optimise_gsd}
\alias{graph_optimise_gsd}
\alias{graph_optimize_gsd}
\title{Optimise graph-based multiple testing procedures for a group sequential
design}
\usage{
graph_optimise_gsd(
  pvals,
  graph_constraint,
  trial_success,
  ...,
  alpha = NULL,
  start_graph = list(list(hyp_weight = NULL, trans_matrix = NULL)),
  global_search = TRUE,
  num_threads = 1L,
  control = multigrain_control(),
  verbose = multigrain_verbosity()
)
}
\arguments{
\item{pvals}{A \code{multigrain_pvals_gsd} object of repeated (or, per
hypothesis, sequential) p-values, created with \code{\link[=transform_pvalues_gsd]{transform_pvalues_gsd()}}.}

\item{graph_constraint}{A \code{multigrain_graph_constraint} object containing
constraints on the graph's weights and transition matrix. Created with
\code{\link[=graph_constraint]{graph_constraint()}}.}

\item{trial_success}{A \code{multigrain_trial_success_gsd} object defining the
trial success measure (gain function) to maximise. Created with
\code{\link[=trial_success_gsd]{trial_success_gsd()}}. A fixed-sample object from \code{\link[=trial_success]{trial_success()}} is
not accepted; write a rejection-only gain such as \code{r1 + r2} with
\code{\link[=trial_success_gsd]{trial_success_gsd()}} instead.}

\item{...}{These dots are for future extensions and must be empty.}

\item{alpha}{A single numeric value representing the overall one-sided
significance level, or \code{NULL} (the default) to use the level of \code{pvals}.
A supplied value must be greater than \code{0} and at most the level the
repeated p-values were built up to: above that they are capped at 1, so
no allocation beyond it could ever reject.}

\item{start_graph}{Optional. Initial list of graphs suggested. Each graph is
defined as a list containing starting weight vector \code{hyp_weight} and
transition matrix \code{trans_matrix}. If \code{NULL}, default starting values are
generated (currently with a Bonferroni-Holm graph).}

\item{global_search}{A logical indicating whether to perform a global
optimisation before the local optimisation. Defaults to \code{TRUE}.}

\item{num_threads}{Number of threads to use for parallel execution of the
shortcut algorithm. On shared systems (HPC clusters, login nodes), always
explicitly set \code{num_threads} based on your resource allocation. Default is
\code{1L} (serial execution).}

\item{control}{An optional \code{multigrain_control} object can be used to set
various graph optimisation parameters. Created with \code{\link[=multigrain_control]{multigrain_control()}}.}

\item{verbose}{An optional string controlling verbosity (\code{"detail"} >
\code{"info"} > \code{"silent"}). Verbosity can also be set at package level with the
\code{multigrain_verbosity} option (see \code{\link[=multigrain_verbosity]{multigrain_verbosity()}}):
\itemize{
\item \code{"info"} (default): will only show milestones / informational messages
highlighting the progress of the optimisation at coarse-grained level.
\item \code{"detail"}: will show milestones and information about fine-grained
optimisation events.
\item \code{"silent"}: no information about the progress of the optimisation is
printed to the console. Errors and warnings are still thrown normally.
}}
}
\value{
A \code{multigrain_graph_optimal} object containing:
\itemize{
\item \code{hyp_weight}: Optimised hypothesis weights (numeric vector).
\item \code{trans_matrix}: Optimised transition matrix (numeric matrix).
\item \code{constraints}: List of constraints used in the optimisation for weights
and transition matrix.
\item \code{trial_success}: The trial success function used in the optimisation.
\item \code{power}: The \code{\link[=calc_power_pvals_gsd]{calc_power_pvals_gsd()}} list for the optimised graph, so it
carries power by analysis, the decision-time distribution and the value
of the trial success measure as well as the fixed-sample power metrics.
\item \code{solution}: A list containing:
\itemize{
\item \code{opt_source}: Source of the optimal solution (\code{local} or \code{global}).
\item \code{graph_valid}: Named logical vector indicating validity of the local
and global solutions (\code{c("local" = TRUE/FALSE, "global" = TRUE/FALSE)}).
}
\item \code{global_search}: \code{TRUE} or \code{FALSE} indicating whether a global
optimisation was performed.
\item \code{control}: A modified \code{\link[=multigrain_control]{multigrain_control()}} object used. The values
passed on by the user are complemented with contextual defaults.
\item \code{global_output}: Output from the genetic algorithm if global
optimisation was performed.
\item \code{local_output}: Output from the NLOPT optimisation.
\item \code{start_graph}: Initial starting values used in the optimisation.
}
}
\description{
Group sequential counterpart of \code{\link[=graph_optimise]{graph_optimise()}}. Optimise the hypothesis
weights and transition matrix of a graph-based multiple testing procedure
for a design with several analyses, using \code{\link[nloptr:nloptr]{nloptr::nloptr()}} local
optimisation and, optionally, a genetic algorithm using \code{\link[GA:ga]{GA::ga()}} for
global optimisation.
}
\details{
The output is a graph where a specified objective function - the
\emph{trial success measure} - is maximised under given constraints on the graph
structure, conditional on the repeated p-values supplied. Because the gain
sees the analysis at which each hypothesis was rejected, an early rejection
can be worth more than a late one.

The search, the encoding and the pruning are those of
\code{\link[=graph_optimise]{graph_optimise()}}. Only the evaluation changes: the group sequential
kernel runs the fixed-sample cascade once per analysis, carrying the
updated graph forward, and reports the analysis at which each hypothesis
was declared rejected. Those decision times are what \code{trial_success}
scores.
}
\examples{
# two hypotheses, an interim at 50\% information and a final analysis
corr <- matrix(c(1, 0.5, 0.5, 1), nrow = 2)

set.seed(1)
raw <- simulate_pvalues_gsd(
    power_nominal = c(0.9, 0.8),
    corr_matrix = corr,
    info_frac = c(0.5, 1),
    nsim = 2000
)

pvals <- transform_pvalues_gsd(
    raw,
    spending = gsDesign::sfLDOF,
    grid_size = 128L
)

# a rejection at the final analysis is worth 75\% of one at the interim
gain <- trial_success_gsd(
    r1 * d(t1) + r2 * d(t2),
    d = c(1, 0.75)
)

\donttest{
result <- graph_optimise_gsd(
    pvals = pvals,
    graph_constraint = graph_constraint_free(2),
    trial_success = gain,
    num_threads = 2
)
}
}
\seealso{
\code{\link[=graph_optimise]{graph_optimise()}} for the fixed-sample version,
\code{\link[=transform_pvalues_gsd]{transform_pvalues_gsd()}} for the p-value object and
\code{\link[=trial_success_gsd]{trial_success_gsd()}} for the gain.
}
