% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_pvals_gsd.R
\name{simulate_pvalues_gsd}
\alias{simulate_pvalues_gsd}
\title{Simulate raw p-values for a group sequential design}
\usage{
simulate_pvalues_gsd(
  power_nominal,
  ...,
  alpha = 0.025,
  corr_matrix = diag(length(power_nominal)),
  info_frac,
  nsim = 1e+05
)
}
\arguments{
\item{power_nominal}{A numeric vector of nominal power values for each
hypothesis, at its final analysis.}

\item{...}{These dots are for future extensions and must be empty.}

\item{alpha}{A number giving the overall one-sided significance level used
to convert \code{power_nominal} into non-centrality parameters. Default is
\code{0.025}.}

\item{corr_matrix}{A numeric matrix representing the correlation matrix
\eqn{\Sigma} of the test statistics at the final analysis.}

\item{info_frac}{Information fractions. Either a numeric vector of length
\code{K}, applied to every hypothesis, or an \code{m} by \code{K} numeric matrix with
\code{NA} at analyses where a hypothesis has no data. They must be positive and
non-decreasing over the analyses where a hypothesis has data, and every
hypothesis must have data at some analysis.}

\item{nsim}{An integer indicating the number of simulations to run. Default
is \code{1e5}.}
}
\value{
A numeric array of raw p-values with dimensions \code{c(nsim, m, K)}:
simulations by hypotheses by analyses. The \code{m} by \code{K} matrix of
information fractions is attached as an \code{info_frac} attribute, where
\code{\link[=transform_pvalues_gsd]{transform_pvalues_gsd()}} picks it up.
}
\description{
\code{simulate_pvalues_gsd()} simulates raw p-values at several analyses
("looks") of a group sequential trial, under the canonical joint normal
model for the test statistics. It is the group sequential counterpart of
\code{\link[=simulate_pvalues]{simulate_pvalues()}}: \code{power_nominal} and \code{corr_matrix} describe the
\emph{final-look} statistics, and the information fractions say how much of that
information is available at each analysis.
}
\details{
The non-centrality parameter \eqn{\Delta_i} of each hypothesis is
obtained from its nominal power with \code{\link[=calc_ncp]{calc_ncp()}}, exactly as in
\code{\link[=simulate_pvalues]{simulate_pvalues()}}. Writing \eqn{t_{i,k}} for the information fraction
of hypothesis \eqn{i} at analysis \eqn{k}, the test statistics follow the
canonical joint distribution

\deqn{E[Z_{i,k}] = \Delta_i \sqrt{t_{i,k}}, \qquad
  \mathrm{Corr}(Z_{i,k}, Z_{j,l}) = \rho_{ij}
  \sqrt{\min(t_{i,k}, t_{j,l}) / \max(t_{i,k}, t_{j,l})},}

where \eqn{\rho_{ij}} is the entry of \code{corr_matrix} for hypotheses
\eqn{i} and \eqn{j}. Two analyses of the same hypothesis at the same
information fraction are therefore perfectly correlated, and two different
hypotheses observed at the same information fraction have correlation
\eqn{\rho_{ij}}. P-values are \eqn{1 - \Phi(Z_{i,k})}.

Only the analyses that carry distinct information are simulated. Once a
hypothesis reaches full information its statistic no longer changes, so
the p-value at the analysis where it matured is copied forward to every
later analysis; drawing those columns instead would make the covariance
singular. An analysis where a hypothesis has no data (\code{NA} information
fraction) before it matures is \code{NA} in the returned array;
\code{\link[=transform_pvalues_gsd]{transform_pvalues_gsd()}} turns that into a repeated p-value of 1, which
is "cannot reject".
}
\examples{
# two hypotheses analysed at 60\% and 100\% of the planned information
corr <- matrix(c(1, 0.5, 0.5, 1), nrow = 2)

set.seed(1)
pvals <- simulate_pvalues_gsd(
    c(0.8, 0.9),
    corr_matrix = corr,
    info_frac = c(0.6, 1),
    nsim = 100
)

dim(pvals)
attr(pvals, "info_frac")

# the second hypothesis has no data at the interim analysis, and the first
# matures there: its p-value is carried forward to the final analysis
set.seed(1)
pvals_na <- simulate_pvalues_gsd(
    c(0.8, 0.9),
    corr_matrix = corr,
    info_frac = rbind(c(1, 1), c(NA, 1)),
    nsim = 100
)

identical(pvals_na[, 1L, 1L], pvals_na[, 1L, 2L])
all(is.na(pvals_na[, 2L, 1L]))
}
