% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_graph_constraint.R
\name{autoplot.multigrain_graph_constraint}
\alias{autoplot.multigrain_graph_constraint}
\alias{plot.multigrain_graph_constraint}
\title{Autoplot method for \code{multigrain_graph_constraint} objects}
\usage{
\method{autoplot}{multigrain_graph_constraint}(object, ..., root = NULL, digits = NULL, title = NULL)

\method{plot}{multigrain_graph_constraint}(x, ..., root = NULL, digits = NULL, title = NULL)
}
\arguments{
\item{object}{A \code{multigrain_graph_constraint} object.}

\item{...}{Additional arguments.}

\item{root}{(integer-like) An optional argument containing a vector of nodes
to plot as root.}

\item{digits}{Number of decimal places to round to (between 0 and 3).
Defaults to \code{NULL} which will choose the number of digits based on how
crowded the graph plot will be:
\itemize{
\item a single digit if more than 8 hypotheses
\item 2 digits if 5 to 8 hypotheses
\item 3 digits if 4 or fewer hypotheses
}}

\item{title}{(string) Optional plot title.}

\item{x}{A \code{multigrain_graph_constraint} object.}
}
\value{
a \code{ggraph} / \code{ggplot} object.
}
\description{
Autoplot method for \code{multigrain_graph_constraint} objects
}
\examples{
library(ggplot2)
gc1 <- graph_constraint(
    c(NA, NA, 0, 0, 0),
    matrix(
       c(
           0, 0.8, 0.2, 0, 0,
           NA, 0, 0, NA, 0,
           NA, 0.8, 0, 0.1, NA,
           0.9, NA, NA, 0, NA,
           0, 0, 0, 1, 0
       ),
       nrow = 5,
       byrow = TRUE
   )
)

autoplot(gc1)

# If you want to control which nodes are treated as root, you can pass them
# as a numeric vector via the `root` argument. For example, we want nodes 1
# and 3 to be plotted as root:
ggplot2::autoplot(gc1, root = c(1, 3))
}
