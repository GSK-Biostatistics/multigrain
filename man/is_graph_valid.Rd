% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{is_graph_valid}
\alias{is_graph_valid}
\title{Check the validity of a graph-based MTP}
\usage{
is_graph_valid(
  hyp_weight,
  trans_matrix,
  sum_to_one_constraint = TRUE,
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{hyp_weight}{A numeric vector representing the weights of the nodes
(hypotheses) in the graph. All elements must be in the interval [0, 1]
and the vector must sum to 1.}

\item{trans_matrix}{A numeric matrix representing the transition matrix
between nodes. All elements must be in the interval [0, 1], with diagonal
elements equal to 0, and each row must sum to 1.}

\item{sum_to_one_constraint}{A logical indicating whether to allow graphs
where both the transition matrix rows are not constrained to sum-to-one,
for example in a fixed sequence. Defaults to \code{TRUE}.}

\item{tolerance}{numeric >= 0. The tolerance when evaluating the sum-to-one
constraints of the hypothesis weights and transition matrix rows. The
default value is close to \code{1.5e-8} - i.e. \code{sqrt(.Machine$double.eps)} (the
standard R definition of  "practically equal", as used by
\code{\link[base:all.equal]{base::all.equal()}}).}
}
\value{
A logical value: \code{TRUE} if the graph is valid, otherwise \code{FALSE}.
The function issues warnings if any of the validity checks fail.
}
\description{
Check that a given weight vector and transition matrix satisfy several
conditions to determine if they form a valid graph. It verifies that the
matrix is square, the dimensions match the length of the weight vector,
the weight vector elements and matrix elements lie within the interval
[0, 1], and that the weight vector sums to 1. Additionally, it checks the
matrix is properly normalised, with all diagonal elements equal to 0 and each
row summing to 1.
}
\details{
The function performs the following checks to determine graph validity:
\itemize{
\item \code{trans_matrix} is a square matrix.
\item the length of \code{hyp_weight} matches the number of rows and columns in
\code{trans_matrix}.
\item all diagonal elements of \code{trans_matrix} are 0.
\item all elements of \code{hyp_weight} and \code{trans_matrix} lie in the interval
[0, 1].
\item the sum of \code{hyp_weight} equals 1.
\item each row of \code{trans_matrix} sums to 1.
}

If any of these conditions are not satisfied, the function returns \code{FALSE}
and issues the corresponding warning message.
}
\examples{
hyp_weight <- c(0.4, 0.3, 0.3)
trans_matrix <- matrix(
                    c(0, 0.5, 0.5, 0.3, 0, 0.7, 0.6, 0.4, 0),
                    nrow = 3,
                    byrow = TRUE
                )
is_graph_valid(hyp_weight, trans_matrix)
}
