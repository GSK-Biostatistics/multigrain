% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_constraint.R
\name{graph_constraint}
\alias{graph_constraint}
\title{Create a \emph{graph constraint} object for optimisation procedures}
\usage{
graph_constraint(
  hyp_constraint = NULL,
  trans_constraint = NULL,
  ...,
  names = "auto",
  diagnose = FALSE,
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{hyp_constraint}{A numeric vector defining the constraints on the
hypothesis weight vector. If \code{NULL}, all hypothesis weights are free
parameters to be optimised.}

\item{trans_constraint}{A numeric matrix defining the constraints on the
transition matrix between hypotheses. If \code{NULL}, all transition matrix
weights are free parameters to be optimised, except for diagonal elements
which remain set to 0.}

\item{...}{These dots are for future extensions and must be empty.}

\item{names}{An optional character vector containing hypotheses' names. If
not provided it defaults to \code{"auto"} meaning the hypotheses will be
automatically named \code{"H1"}, \code{"H2"}, etc.}

\item{diagnose}{A logical value enabling detailed diagnosis. Default is
\code{FALSE}.}

\item{tolerance}{numeric >= 0. Differences smaller than \code{tolerance} will not
be reported. The default value is close to \code{1.5e-8} -
i.e. \code{sqrt(.Machine$double.eps)} (the standard R definition of
"practically equal", as used by \code{\link[base:all.equal]{base::all.equal()}}).}
}
\value{
A multigrain \emph{graph constraint} object (an S3 list with class
\code{multigrain_graph_constraint}) containing:
\itemize{
\item \code{hyp_constraint}: a numeric vector representing the constraints on the
hypothesis weight vector.
\item \code{trans_constraint}: a numeric matrix representing the constraints on the
transition matrix.
If an element is \code{NA}, it is a free parameter to be optimised by
\code{graph_optimise()}.
}
}
\description{
A \emph{graph constraint} object defines constraints on the hypothesis weight
vector and transition matrix for optimisation of graph-based multiple testing
procedures.
}
\details{
The object contains a constraint on the weights (\code{hyp_constraint}) and a
constraint on the transition matrix (\code{trans_constraint}).

The \emph{graph constraint} object is used to define constraints on both the
hypothesis weight vector and the transition matrix in graph-based
optimisation procedures. The \code{graph_optimise()} function will read the graph
constraints and only optimise free parameters (specified by \code{NA} in
\code{hyp_constraint} and \code{trans_constraint}).

Either \code{hyp_constraint} or \code{trans_constraint} must be provided (they can't
both be \code{NULL} at the same time). If only one is provided, the
\emph{graph constraint} object will allow \code{graph_optimise()} to optimise any
parameter (i.e., no constraints will be specified) in the other one.

\code{hyp_constraint} and \code{trans_constraint} must refer to the same number of
hypotheses.
}
\examples{
# Create a graph_constraint object with predefined weight constraints
graph_constraint(hyp_constraint = c(NA, 0.4, NA))

}
\references{
Xi, D. and Chen, Y. (2024). Optimal weighted Bonferroni tests and
their graphical extensions. \emph{Statistics in Medicine}, 43(3),
475--500. \url{https://doi.org/10.1002/sim.9958}
}
