% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{normalise_sum}
\alias{normalise_sum}
\title{Normalise graph weights to sum to a target value}
\usage{
normalise_sum(
  x,
  ...,
  fixed_idx = integer(0),
  target = 1,
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{x}{A numeric vector of weights to be normalised.}

\item{...}{These dots are for future extensions and must be empty.}

\item{fixed_idx}{An integer vector representing the indices of elements that
must not be modified (e.g. diagonal entries in a transition matrix, or
positions locked by \code{\link[=graph_constraint]{graph_constraint()}}). Defaults to \code{integer(0)} (no
fixed elements).}

\item{target}{A number representing the desired sum for the output vector.
Default is \code{1}.}

\item{tolerance}{numeric >= 0. The tolerance to be used when checking whether
the sum has converged to \code{target}. The default value is close to \code{1.5e-8} -
i.e. \code{sqrt(.Machine$double.eps)} (the standard R definition of
"practically equal", as used by \code{\link[base:all.equal]{base::all.equal()}}).}
}
\value{
A numeric vector of the same length as \code{x}, adjusted so that
\code{sum(x)} is within \code{tolerance} of \code{target}.
}
\description{
Ensures that a numeric vector of graph weights is rescaled so its elements
sum to a target value (default 1). This is useful when exporting optimised
graphs (e.g. to gMCP), where weights must form a valid probability vector.
}
\details{
Many downstream tools (e.g. gMCP) require hypothesis weights to sum to 1.
Direct division by \code{sum(x)} may leave tiny discrepancies due to
floating-point rounding. This helper fixes such issues automatically,
ensuring exported graphs are valid.

The algorithm first scales all free (non-fixed) elements proportionally so
they sum to \code{target - sum(x[fixed_idx])}. It then computes the largest
free element (the "anchor") as the exact complement of all other elements
(\code{target - sum(x[-anchor])}), absorbing any remaining rounding residual. A
single additive fallback pass handles the rare case where the prior scaling
introduced enough rounding for the complement to land 1 ULP off.
}
\note{
Two edge cases return early without enforcing \code{target}:
\itemize{
\item If all elements of \code{x} are zero, the zero vector is returned
unchanged (proportional scaling is undefined for an all-zero input).
\item If every index is in \code{fixed_idx} (no free elements), the input
is returned as-is — the caller is responsible for ensuring
\code{sum(x) == target} when no elements may be modified.
}
}
\examples{

x <- c(0.2, 0.3, 0.500000001)
print(sum(x) == 1)

x_sum_to_1 <- normalise_sum(x)
print(all.equal(sum(x_sum_to_1), 1))

# With fixed elements: indices 1 and 3 are held constant
x2 <- c(0.25, 0.45, 0.30)
normalise_sum(x2, fixed_idx = c(1L, 3L))
}
